package database

import org.specs2.mutable._

class MultipleExceptionTest extends Specification {

  "MultipleException" should {
    /*
  "report all errors" in {
    val me = MultipleException(Seq(new RuntimeException("foo"), new Exception("bar")))
    me.toString must contain("foo")
    me.toString must contain("bar")
    }
    */
  }
  
}
